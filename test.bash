_cli_completion() {
	    local _clap_line="${COMP_LINE}"
	        # The vulnerable line introduced in PR 6364:
		    eval "local _clap_words=($_clap_line)"
		     }
		     complete -F _cli_completion cli
